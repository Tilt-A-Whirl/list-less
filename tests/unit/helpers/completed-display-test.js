import { completedDisplay } from '../../../helpers/completed-display';
import { module, test } from 'qunit';

module('Unit | Helper | completed display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = completedDisplay(42);
  assert.ok(result);
});
