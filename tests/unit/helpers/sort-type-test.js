import { sortType } from '../../../helpers/sort-type';
import { module, test } from 'qunit';

module('Unit | Helper | sort type');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sortType(42);
  assert.ok(result);
});
