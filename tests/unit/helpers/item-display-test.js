import { itemDisplay } from '../../../helpers/item-display';
import { module, test } from 'qunit';

module('Unit | Helper | item display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = itemDisplay(42);
  assert.ok(result);
});
