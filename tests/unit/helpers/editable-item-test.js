import { editableItem } from '../../../helpers/editable-item';
import { module, test } from 'qunit';

module('Unit | Helper | editable item');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = editableItem(42);
  assert.ok(result);
});
